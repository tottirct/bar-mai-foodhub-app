export interface OwnerInfo {
    name: string;
    username: string;
    email: string;
}

export interface ShopInfo {
    name: string;
    description: string;
    isOpen: boolean;
    imageUrl: string;
}
